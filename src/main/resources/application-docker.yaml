

spring:
  application:
    name: k8s-demo-spring-boot
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    
